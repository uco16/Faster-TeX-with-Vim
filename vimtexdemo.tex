\documentclass[12pt]{article}

\usepackage{amsmath}
\usepackage{physics}

\begin{document}

% --- UltiSnips EXAMPLES ----

% many custom commands like '::'

% define on the fly: e.g. want ... -> \dots
% editing shortcuts (<leader><leader>tms)

% Greek indices with '.' or '..'

% Math mode specific commands

% underbrace and overbrace

% short time maximisations like products: wedge, dot, star, otimes, bigoplus (Oxx), sum

% Matrices

% --- vimtex additions ---

% change surrounding environment 'cse'

\begin{align}
  I &= (\int f(\vb{x}) \dd[4]{x}) \\
    &= \bra{f} (S-1) \ket{i}
\end{align}

% toggle surrounding delimiter

% change in dollars 'di$' / 'ci$'
$2 + 5 = 4$.

\end{document}
